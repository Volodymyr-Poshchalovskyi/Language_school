/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* Since standard react code has JSX without imports or uses JSX extension, Next.js handles it natively */
};

export default nextConfig;
